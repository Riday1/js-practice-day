const learn = ['html', 'css', 'bootstrap', 'tailwind', 'javaScript', 'variable', 'keyword', 'condition', 'loop', 'array', 'function'];

for (var i = 0; i < learn.length; i++) {
    console.log(learn[i])
}